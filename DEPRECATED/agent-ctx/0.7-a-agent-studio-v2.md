# Task 0.7-a: Update agent-studio.tsx with V2 Improvements

## Summary

Updated `/home/z/my-project/src/components/agent/agent-studio.tsx` with all requested V2 improvements.

## Changes Made

### 1. SSE Auto-Reconnect (FIX C15)
- Added exponential backoff reconnection logic with constants: `MAX_RECONNECT_ATTEMPTS=5`, `BASE_RECONNECT_DELAY_MS=1000`, `MAX_RECONNECT_DELAY_MS=30000`
- Implemented `attemptReconnect()` function that retries with increasing delay (1s, 2s, 4s, 8s, 16s capped at 30s)
- Used `connectSSERef` to avoid circular dependency between `attemptReconnect` and `connectSSE`
- Reconnect counter resets on successful message receipt
- After max retries, sets error state with descriptive message

### 2. URL Encoding Fix
- Changed SSE URL from `/api/agent/stream?runId=${currentRunId}` to `/api/agent/stream?runId=${encodeURIComponent(runId)}`

### 3. Error State Display
- Imported `error` and `clearError` from the store
- Added `useEffect` that shows a destructive toast notification whenever `error` is set
- Added `handleClearError` callback that clears error on user interaction (click on main area)

### 4. Delete Agent Support
- Imported `deleteAgent` from the store — it's now accessible via destructuring for any child components that need it through the store

### 5. V2 Event Types
- Added `V2_EVENT_TYPES` set containing all event types: "status", "plan", "action", "result", "thinking", "error", "done", "reflection", "policy", "handoff", "group_message"
- SSE `onmessage` handler now validates event types against this set and also handles unknown types for forward compatibility
- All V2 types are passed to `addEvent()`
- Special handling for `"error"` type events (sets store error state)

### 6. Framer Motion Panel Transitions
- Added `AnimatePresence` + `motion.div` wrappers for sidebar and inspector panels
- Panels animate with width/opacity transitions (duration: 0.2s, easeInOut)

### 7. Import Updates
- Added `motion`, `AnimatePresence` from `framer-motion`
- Added `useToast` from `@/hooks/use-toast`
- Removed unused `WifiOff` icon (was in early draft)
- All store imports remain compatible with the updated `AgentEvent` type

## Lint Status
✅ All lint checks pass with zero errors.
